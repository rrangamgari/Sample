package com.example.demo.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.demo.model.WebServiceActivity;

public interface WebServiceActivityRepository extends JpaRepository<WebServiceActivity, Long> {
	  //~ Methods ----------------------------------------------------------------------------------------------------------

	  
	}